# ============================================================
# localplay — .gitignore
# ============================================================

# --- Rust / Cargo ---
# NOTE: Cargo.lock IS committed (this is a binary application, not a library).
target/
**/target/
**/*.rs.bk
*.pdb
*.profraw
*.profdata

# --- Node / frontend toolchain ---
node_modules/
**/node_modules/
.pnpm-store/
.yarn/
dist/
build/
.vite/
*.tsbuildinfo
.eslintcache
# The desktop app's two generated directories. The patterns above already match them at any
# depth; they are named here so that `apps/desktop/` cannot be committed by accident by
# someone reading this file and concluding only the repository root is covered.
apps/desktop/node_modules/
apps/desktop/dist/
# Playwright's own output directories, in case a future run adds a @playwright/test config
# beside the `npm run shots` script (whose screenshots and browser download already live
# under the ignored target/).
playwright-report/
test-results/

# --- Tauri ---
src-tauri/gen/schemas/
**/src-tauri/gen/
**/src-tauri/target/
*.app
*.dmg
*.msi
*.AppImage
*.deb
*.rpm

# --- Bundled / sidecar binaries ---
# ffmpeg & ffprobe ship as extracted sidecars, never committed.
binaries/
**/binaries/*
!**/binaries/.gitkeep
ffmpeg
ffmpeg.exe
ffprobe
ffprobe.exe
*.exe
*.dll
*.dylib
*.so
*.so.*
*.lib
*.a
!**/*.node

# --- Video caches, scratch disks & recordings ---
# Never commit media: captures, ring-buffer scratch files, clips, thumbnails.
recordings/
clips/
captures/
thumbnails/
scratch/
ringbuffer/
**/scratch/
*.mp4
*.mkv
*.mov
*.webm
*.avi
*.flv
# MPEG transport streams — NOT a blanket `*.ts`. That pattern was here, and it silently
# ignored every TypeScript file in the repository as well; the desktop app is written in
# TypeScript, so the rule is scoped to the directories media can actually land in.
clips/**/*.ts
captures/**/*.ts
recordings/**/*.ts
scratch/**/*.ts
ringbuffer/**/*.ts
*.m4s
*.m2ts
*.part
*.tmp
*.temp
*.cache
# Explicitly keep small test fixtures if they ever land in tree.
!**/fixtures/**/*.txt
!**/fixtures/**/*.json

# --- Local database (SQLite index of matches/clips) ---
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite3-journal

# --- Secrets & environment ---
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
secrets/
credentials.json
# Windows DPAPI / local config with tokens
*.dpapi

# --- Logs & diagnostics ---
logs/
*.log
crash-*.dmp
report.*.json

# --- OS cruft ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# --- Editors / IDE ---
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
*.swp
*.swo
*~
.claude/settings.local.json
