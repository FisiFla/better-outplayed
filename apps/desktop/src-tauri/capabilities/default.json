{
  "identifier": "default",
  "description": "The default capability for the localplay review window: the core Tauri APIs plus the open-file dialog the settings panel's folder picker uses (open only — it returns a path and grants nothing else). There is no filesystem, shell or http permission here — clip files reach the webview through the asset protocol, whose scope is configured in tauri.conf.json and extended at runtime in src/lib.rs to the resolved clips directory.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
